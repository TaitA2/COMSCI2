attach(chickwts)
boxplot(weight ~ feed)
boxplot(weight ~ feed, xlab="Feed Type", ylab="Chick Weight (grams)", main="A comparison of chick weight to feed type")
boxplot(weight ~ feed, xlab="Feed Type", ylab="Chick Weight (grams)", main="A comparison of chick weight to feed type")
boxplot(weight ~ feed, xlab="Feed Type", ylab="Chick Weight (grams)", main="A comparison of chick weight to feed type")
tapply(weight, feed, mean)
tapply(weight, feed, var)
light_feeds = feed[weight < 159]
lin_or_sun = light_feeds[light_feeds=="linseed" | light_feeds == "sunflower"]
proportion = length(lin_or_sun) / length(light_feeds)
proportion
n_iterations = 10000
result = 0
for (i in c(1:n_iterations){
n_iterations = 10000
result = 0
for (i in c(1:n_iterations)){
j = 0
while (runif(1) > 0.2){
j = j + 1
}
result[i] = j
}
length(result[result == 5]) / n_iterations
